# Services

Core business orchestration layer.

Flow:
- Initialized from CLI
- Uses container for dependencies

Rules:
- No string output
- No direct external calls
- Must be initialized before use